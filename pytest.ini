[pytest]
addopts = -v -s --tb=short --html=artifacts/report.html --self-contained-html
testpaths =
    web_automation/tests
    mobile_automation/tests
python_files = test_*.py
markers =
    smoke: quick high-value checks
    negative: invalid/edge input tests
